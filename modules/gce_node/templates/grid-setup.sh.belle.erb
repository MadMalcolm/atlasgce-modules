# Keep grid setup out of environment for root and sysadmin.
if [[ ! "$USER" =~ ^slot[0-9]+$ ]] ; then
  return 0
fi

#export BELLE2_SYSTEM_PYTHON=1
#export BELLE2_FILECATALOG=NONE
export VO_BELLE_SW_DIR=/cvmfs/belle.cern.ch/sl6
